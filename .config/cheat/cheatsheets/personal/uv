---
syntax: bash
tags: [ python, environments ]
---

# install new python version
uv python install <version>

# add package to project
uv add <package>

# view outdated packages in project
uv tree --outdated --depth=1

# upgrade all outdated packages in project
uv lock --upgrade
uv sync

# install a tool from github
uv tool install git+ssh://git@github.com/<user>/<package>.git

# upgrade tool
uv tool upgrade <package>

# uninstall tool
uv uninstall <package>

# completely empty cache
uv cache clean

# remove unreachable objects from cache
uv cache prune

# run script
uv run example.py

# Example of inline script metadata
# Add the following (incl. pounds) to the top of a script, chmod 744, uv run ./script.py
# from: https://treyhunner.com/2024/12/lazy-self-installing-python-scripts-with-uv/

#!/usr/bin/env -S uv run --script
# /// script
# requires-python = ">=3.13"
# dependencies = [
#   "click",
#   "rich-click",
#   "git-lib"
# ]
#
# [tool.uv.sources]
# git-lib = { git = "https://github.com/username/git-lib" }
#
# [tool.uv]
# exclude-newer = "2025-02-05T00:00:00Z"
# ///

# Generate script file with some inline metadata placeholders
uv init --script example.py --python 3.13
