---
syntax: bash
tags: [ mac, deps ]
---
# Update
brew update

# Upgrade everything
brew upgrade

# Upgrade specific formula
brew upgrade <formula>

# Install formula
brew install <formula>

# Install cask
brew install --cask <formula>

# "Adopt" existing cask install
brew install --cask --adopt <formula>

# Uninstall
brew uninstall <formula>

# List installed formulae
brew list

# Fix
brew doctor

# Clean up
brew cleanup

# List installed formulae and descriptions, without dependencies
brew leaves | xargs brew desc --eval-all

# create a Brewfile for all forumlae, casks and mas
# force to write to file if exists and include description
brew bundle dump --force --describe

# View outdated forumula
brew outdated

# View dependencies for all installed formula
brew deps --installed --tree $(brew leaves)
